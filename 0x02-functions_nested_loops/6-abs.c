#include "main.h"

/**
 * _abs-prints the absolute value of an integer
 * * @k:the integer to compute its absolute value
 * Return:returns the abs value
 */
int _abs(int k)
{
	if (k >= 0)
		return (k);
	else
		return (-k);
}
